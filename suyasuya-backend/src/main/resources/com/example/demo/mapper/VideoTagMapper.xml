<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.VideoTagMapper">

    <select id="selectByName" resultType="videoTag">
        SELECT * FROM video_tags WHERE tag_name = #{tagName}
    </select>

    <!--查询视频标签-->
    <select id="selectTagsByVideoId" resultType="java.lang.String">
        SELECT t.tag_name FROM video_tags t
            INNER JOIN video_tag_relations r ON t.tag_id = r.tag_id
                WHERE r.video_id = #{videoId}
    </select>

    <!--标签热度更新-->
    <update id="incrementRelatedCount">
        UPDATE video_tags SET related_count = related_count + 1 WHERE tag_id = #{tagId}
    </update>

</mapper>